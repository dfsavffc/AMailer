meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  dockerfile: Dockerfile
  context: anonymous-mailer
  skip: false

run:
  persistenceMount: /data
  containerPort: 8000